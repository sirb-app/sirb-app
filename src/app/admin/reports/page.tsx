export default function Page() {
  // TODO:
  // - Primary goal: List and triage user reports; allow moderators to act on reports within their scope.
  // - Data/entities: reports, related content, reporter, report status, assigned moderator.
  // - Permissions: ADMINs see all; moderators scoped to subjects they moderate.
  return null;
}
